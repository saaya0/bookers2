<div class="container">
	<div class="row">
		<div class="col-lg-3"></div>
		<div class="col-lg-6">
			<h1>Editing Book</h1>


			<% if @book.errors.any? %>
  			<h3><%= @book.errors.count %>error prohibited this obj from being saved:</h3>
 				・<% @book.errors.full_messages.each do |message| %>
    			<%= message %>
 			 	<% end %>
			<% end %>


			<%= form_with model: @book, local:true do |f| %>
        <h4>title</h4>
        <%= f.text_field :title %>
        <h4>opinion</h4>
        <%= f.text_field :body %>
        <div class="edit-btn"><%= f.submit 'Update Book', class: "btn btn-success" %></div>
					<%= link_to "Show", book_path(@book) %>	|
					<%= link_to "Back", books_path %>

			<% end %>



		</div>
				<div class="col-lg-3"></div>
	</div>
</div>